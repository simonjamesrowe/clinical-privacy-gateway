[package]
name = "clinicians-veil-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "clinicians_veil_core"
path = "src/lib.rs"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
